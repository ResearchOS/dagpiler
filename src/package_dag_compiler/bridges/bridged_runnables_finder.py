from bridges.bridged_package_finder import BridgedPackageFinder

class BridgedRunnablesFinder:

    def __init__(self, bridged_runnables_dict: dict):
        # Each key is one bridge, and the values are "sources" and "targets" keys.
        self.bridged_runnables_dict = bridged_runnables_dict

    def get_runnables_from_dependencies():
        """Get the runnables from the bridged (dependency) packages."""
        pass